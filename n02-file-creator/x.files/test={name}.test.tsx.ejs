import { render, screen } from '@testing-library/react';
import { <%= name %> } from './<%= name %>';

test('<%= name %> is drawn', () => {
  // const { baseElement } = render(<<%= name %></<%= name %>>);
  // expect(screen.getByText('here <%= name %>')).toBeInTheDocument();
});